<?php
/**
 * =============================================================================
 *  Конфигурация сервера — СЕРВИСНЫЕ КЛЮЧИ VK API
 * =============================================================================
 *  ВНИМАНИЕ: этот файл хранится ТОЛЬКО на сервере и не должен попадать
 *  в браузер клиента. Файл добавлен в .gitignore.
 *
 *  Ключи проверены на работоспособность (методы users.get, groups.getById)
 *  11.09.2026.
 *
 *  Совместимо с PHP 7.4+
 */

return [

    // Основной сервисный ключ доступа VK (проверен — рабочий)
    'vk_service_token' => '1543ce801543ce801543ce80d0167df366115431543ce807c1370050b48ab4c01eabc6a',

    // Резервный сервисный ключ — используется автоматически, если основной
    // будет отозван или вернёт ошибку авторизации (проверен — рабочий)
    'vk_service_token_fallback' => 'd306a4b4d306a4b4d306a4b46ad0389840dd306d306a4b4ba56aeabaf84c50097d998b5',

    // Версия VK API, используемая по умолчанию
    'api_version' => '5.131',

    // ---------------------------------------------------------------------
    // Самообновление с GitHub (вкладка «Настройки»)
    // ---------------------------------------------------------------------

    // Репозиторий и ветка, откуда проверяются и скачиваются обновления
    'github_repo' => 'giddammit-crypto/vk-wall-searcher',
    'github_branch' => 'main',

    // Пароль для запуска обновления из интерфейса (обязателен!).
    // Хранится только на сервере; показывается подсказкой в настройках.
    'update_token' => '1Radio14881!',

    // Необязательный персональный токен GitHub — снимает лимит 60 запросов/час
    'github_token' => '',

];
